# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.data*.json
*.tmp.py
*.tmp.*

npm-debug.log*
yarn-debug.log*
yarn-error.log*

**__pycache__*
bin/brython-libs/*
/no-docs
.uuids

bin/*/feedbacks

.history
.env

secure/
# Want to not track changes of a file, but keep in in git?
# - First change the file you do not want to be tracked and use the following command:

# git update-index --assume-unchanged FILE_NAME

# - and if you want to track the changes again use this command:

# git update-index --no-assume-unchanged FILE_NAME